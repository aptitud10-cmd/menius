import { MediaGallery } from '@/components/dashboard/MediaGallery';

export const metadata = {
  title: 'Galería de imágenes | Menius',
};

export default function MediaPage() {
  return <MediaGallery />;
}
